services:
  agent-api:
    container_name: agentic-cia
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - agentic-cia-network

volumes:
  data:

networks:
  agentic-cia-network:
    name: agentic-cia-network